"""Minimal shared type definitions.

This module contains only fundamental types that are truly shared across multiple
modules. Avoid adding types that belong to a specific domain (use that domain's
types instead).
"""
# Currently empty - types that were here have moved to their proper domains:
# - RAGState, UIComponentType, UIComponentData -> shared/ui_components.py
# - API schemas -> api/schemas.py
