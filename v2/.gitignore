# Build / cache
__pycache__/
*.pyc
.pytest_cache/
.import_linter_cache/
.venv/
*.egg-info/
dist/
build/

# Frontend
web/node_modules/
web/dist/

# User data — never tracked, lives in --data-dir / ANCHOR_DATA_DIR
data/
# Note: top-level v2/docs/ IS tracked — that's the architecture documentation,
# not user data. The user-data canvases/ live inside data/ which is already
# ignored above.

# Editor
.vscode/
.idea/

# OS
.DS_Store
.playwright-cli/
